import styled from '@emotion/styled';
import { ReactComponent as LinkedIn } from '';
import { ReactComponent as GitHub } from '';
import { ReactComponent as UpWork } from '';
import { ReactComponent as Codewars } from '';

const LinkedInIcon = styled(LinkedIn)``;
const GitHubIcon = styled(GitHub)``;
const UpWorkIcon = styled(UpWork)``;
const CodewarsIcon = styled(Codewars)``;

export const styles = {};
